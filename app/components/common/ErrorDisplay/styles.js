import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    justifyContent: 'flex-end',
    left: 0,
    bottom: 0,
    alignSelf: 'stretch',
  },
});
